import { Placeholder } from '../components/Placeholder';

export default function WhatWeDo() {
  return (
    <Placeholder
      eyebrow="What we do"
      title="Creating the conditions to grow."
      blurb="The What We Do page is being built from the Figma design next — how we expose, enrich, and transfer each seed into its own garden."
      height={1500}
    />
  );
}
