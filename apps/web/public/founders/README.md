# Founder headshots

Drop headshot image files here; they are served at the site root.

- `jhaanvi.jpg` → shown on the landing page for Jhaanvi Hiremath
  (referenced in Landing.tsx as `/founders/jhaanvi.jpg`).

Square images (e.g. 400×400) look best — they're cropped to a circle.
If a file is missing, the card falls back to the founder's initials.
