/**
 * @reveal/shared/v2 — the v2.0.0 vocabulary and report-payload contract,
 * shared by the deterministic engine (apps/api) and the report UI (apps/web).
 */
export * from './vocab.js';
export * from './payload.js';
